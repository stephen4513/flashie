import React from 'react';
import { Box } from '@mui/material';
import styles from './carrousel.module.css';

const Carrousel = () => {
    return (
        <Box className={styles.carrouselContainer}>
            {/* Carrousel content will go here */}
        </Box>
    );
};

export default Carrousel;